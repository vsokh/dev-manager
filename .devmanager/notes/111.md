# Task 111: Extract useSync hook

## Manager notes
Extract file I/O, state merging, conflict handling, and sync polling logic from useProject.ts into a dedicated useSync hook that useProject composes.

## Exploration findings
- `useProject.ts` (398 lines) is the main hook. Already extracted: `useConnection.ts` (57 lines), `useQueueActions.ts`, `useTaskActions.ts`
- Sync-related code spans:
  - `mergeProgressIntoState()` pure function (lines 29–115) — progress merge logic
  - `MergeResult` interface (lines 20–27)
  - `handleWsMessageRef` message handler (lines 141–194) — handles 'state', 'progress', 'quality', 'project-switched'
  - `save()` function (lines 273–293) — debounced write with conflict detection
  - `saveTimer` ref (line 132) and `lastWriteTime` ref (line 133)
  - `pauseTask` (lines 295–319) — reads progress, deletes progress file, writes state with lastWriteTime
  - `cancelTask` (lines 326–339) — deletes progress file, writes state with lastWriteTime
  - `disconnect` (lines 258–271) — flushes pending save on disconnect

## Proposed plan
1. Create `src/hooks/useSync.ts` containing:
   - Move `mergeProgressIntoState()` and `MergeResult` interface
   - `useSync` hook that manages: `saveTimer` ref, `lastWriteTime` ref, `dataRef` ref
   - `save()` function with debounce + conflict handling
   - `handleSyncMessage()` — processes 'state' and 'progress' WebSocket messages
   - `pauseTask()` and `cancelTask()` — they're tightly coupled to sync (they read/delete progress files and write state with lastWriteTime tracking)
   - `flushPendingSave()` — for disconnect cleanup

2. Update `useProject.ts`:
   - Import and compose `useSync`
   - Remove all extracted code
   - Wire useSync's `handleSyncMessage` into the WS message handler
   - Use useSync's `save`, `pauseTask`, `cancelTask`, `flushPendingSave`

## Files to modify
- `src/hooks/useSync.ts` — NEW: extracted sync hook
- `src/hooks/useProject.ts` — remove sync logic, compose useSync
