# Task 115: Extract task filtering utils

## Manager notes
None provided.

## Exploration findings
- Epic color calculation duplicated 3 times across 2 files (TaskBoard.tsx lines 48-65, 67-83; EpicField.tsx lines 14-21)
- Status filtering (pending/done/backlog splits) duplicated in TaskBoard.tsx (lines 43-45), EpicGroup.tsx (lines 69-70, 79), useQueueActions.ts (lines 58, 71)
- Task grouping-by-group logic duplicated 3 times in TaskBoard.tsx (lines 106-136)
- "Unqueued pending tasks" filter duplicated in TaskBoard.tsx (line 324), EpicGroup.tsx (line 79), useQueueActions.ts (lines 58, 71)

## Proposed plan
1. Create `src/utils/taskFilters.ts` with:
   - `getPendingTasks(tasks)` - tasks not done/backlog
   - `getDoneTasks(tasks)` - done tasks
   - `getBacklogTasks(tasks)` - backlog tasks
   - `getUnqueuedTasks(tasks, queue)` - pending/paused not in queue
   - `groupTasksBy(tasks, opts?)` - generic Map-based grouping with optional hidden filter
   - `getEpicColors(epics, allGroups)` - the full epic color map calculation
   - `getEpicColorMap(epics)` - simpler version for EpicField
   - `getEpicStats(tasks, allGroups)` - epic progress stats

2. Update consumers:
   - TaskBoard.tsx: replace inline useMemos with imported utils
   - EpicField.tsx: replace inline epicColorMap with imported util
   - EpicGroup.tsx: replace inline status filters with imported utils
   - useQueueActions.ts: replace inline pending filters with imported utils

## Files to modify
- src/utils/taskFilters.ts — NEW: shared filtering/grouping utilities
- src/components/TaskBoard.tsx — replace inline logic with util calls
- src/components/detail/EpicField.tsx — replace epicColorMap with util
- src/components/board/EpicGroup.tsx — replace status filters with utils
- src/hooks/useQueueActions.ts — replace pending filters with utils
