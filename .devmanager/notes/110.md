# Task 110: Extract useConnection hook
## Manager notes
(none)
## Exploration findings
- `useProject.ts` (415 lines) is the main hook
- WebSocket logic spans lines 138, 190-258: `wsRef`, `setupWs()`, reconnection timer, message routing
- `ConnectionStatus` type defined at line 19: 'disconnected' | 'connecting' | 'connected' | 'synced' | 'error' | 'template-picker'
- `connectWebSocket` imported from `api.ts` — creates WS, parses JSON messages, handles close
- WebSocket effect triggers when `connected` becomes true
- Messages handled: 'state', 'progress', 'quality', 'project-switched'
- Reconnection: on close, waits 3s and re-creates
- `disconnect` callback closes WS at lines 281-284

## Proposed plan
1. Create `src/hooks/useConnection.ts` with:
   - `ConnectionStatus` type (moved from useProject)
   - `useConnection` hook managing: `connected`, `status`, `wsRef`, WS effect, reconnect, cleanup
   - Accept `onMessage` callback for message routing
   - Expose `connected`, `setConnected`, `status`, `setStatus`, `closeWebSocket`
2. Update `useProject.ts` to import and use `useConnection`
3. Remove extracted code from `useProject.ts`
4. Ensure existing tests still pass

## Files to modify
- src/hooks/useConnection.ts — NEW: extracted WebSocket + connection state hook
- src/hooks/useProject.ts — Remove WS logic, import useConnection
