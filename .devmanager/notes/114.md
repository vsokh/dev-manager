# Task 114: Extract App.tsx layout concerns

## Manager notes
(none)

## Task description
Extract tab routing, modal state, and floating scratchpad from App.tsx into focused hooks or sub-components.

## Exploration findings

App.tsx is 340 lines. Three main concerns to extract:

### 1. Scratchpad (biggest win ~80 lines)
**State:** `showScratchpad`, `splitting`, `splitResult`, `splitResultTimer` (lines 48-51)
**Logic:** `handleSplitTasks` async handler (lines 53-98) — calls api.splitTasks, updates data, auto-arranges
**JSX:** Floating button + panel (lines 288-337) — fixed position overlay

### 2. Tab routing (small but explicit ask)
**State:** `productTab` (line 44)
**Logic:** Tab switching + clear selection on quality tab (line 187)
**JSX:** Tab bar (lines 179-202), conditional board/quality rendering (lines 204-273)

### 3. Modal state (simple boolean)
**State:** `showSkillsConfig` (line 45)
**JSX:** SkillsConfigPanel conditional rendering (lines 275-283)

### Already extracted
- Undo/redo → `useUndo` hook (line 106)
- Task actions → `useTaskActions` hook (line 108)
- Queue actions → `useQueueActions` hook (line 109)

## Proposed plan
1. Create `useScratchpad` hook — extract all scratchpad state + `handleSplitTasks` logic
2. Create `FloatingScratchpad` component — extract floating button + panel JSX (lines 288-337)
3. Create `useTabRouting` hook — extract `productTab` state + `switchTab` handler
4. Refactor App.tsx to use these new extractions — should reduce to ~220 lines

## Branch
task-114-extract-app-layout-concerns
## Worktree
.devmanager/worktrees/task-114

## Status
- [ ] Create `useScratchpad` hook
- [ ] Create `FloatingScratchpad` component
- [ ] Create `useTabRouting` hook
- [ ] Refactor App.tsx to use new extractions
- [ ] Build passes

## Files to modify
- `src/hooks/useScratchpad.ts` — new hook
- `src/components/FloatingScratchpad.tsx` — new component
- `src/hooks/useTabRouting.ts` — new hook
- `src/App.tsx` — refactor to use new hooks + component
