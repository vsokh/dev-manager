# Task 117: Extract shared addActivity hook
## Manager notes
(none)
## Exploration findings
- `src/hooks/useTaskActions.ts` lines 22-26: `addActivity` helper
- `src/hooks/useQueueActions.ts` lines 31-35: identical `addActivity` helper
- Both functions: take `(label: string, taskId?: number)`, create Activity entry, prepend to current activity array, slice to 20
- Both depend on `data?.activity` from their closure
- Activity type defined in `src/types/index.ts` lines 68-73
- Existing utils directory at `src/utils/`

## Proposed plan
1. Create `src/utils/activityUtils.ts` with a `createActivityList` utility function
2. Update `useTaskActions.ts` to import and use the shared utility
3. Update `useQueueActions.ts` to import and use the shared utility

## Files to modify
- src/utils/activityUtils.ts — NEW: shared addActivity utility
- src/hooks/useTaskActions.ts — replace local addActivity with import
- src/hooks/useQueueActions.ts — replace local addActivity with import
