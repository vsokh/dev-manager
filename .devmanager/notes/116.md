# Task 116: Reduce TaskBoard memoization
## Manager notes
Simplify TaskBoard.tsx memoization strategy. After extracting shared filtering utils (task 115), audit remaining memos — remove unnecessary ones, consolidate related ones.

## Exploration findings
- TaskBoard.tsx has 12 useMemo calls (down from 37 before task 115 extractions)
- No useCallback calls
- Component is NOT wrapped in React.memo
- Child components (EpicGroup, DoneSection) are NOT wrapped in React.memo

### Current useMemo inventory (lines 44-109):
1. L44: `pendingTasks` = getActiveTasks(tasks) — [tasks]
2. L45: `backlogTasks` = getBacklogTasks(tasks) — [tasks]
3. L46: `doneTasks` = getDoneTasks(tasks) — [tasks]
4. L47: `allGroups` = getAllGroups(tasks) — [tasks]
5. L49: `epicColors` = getEpicColors(epics, allGroups) — [allGroups, epics]
6. L69: `epicNames` = (epics||[]).map(e => e.name) — [epics] ← TRIVIAL, remove
7. L70-72: `hiddenEpicNames` = Set of hidden epic names — [epics]
8. L79-89: `filteredPendingTasks` = filter by status + search — [pendingTasks, activeFilter, searchText]
9. L90: `pendingGroups` = groupTasksBy(filtered, {hiddenEpics}) — [filteredPendingTasks, hiddenEpicNames]
10. L91: `doneGroups` = groupTasksBy(doneTasks, {defaultGroup:'Other'}) — [doneTasks]
11. L92: `backlogGroups` = groupTasksBy(backlogTasks, {defaultGroup:'Other'}) — [backlogTasks]
12. L109: `epicStats` = getEpicStats(tasks, allGroups) — [tasks, allGroups]

## Proposed plan
1. Remove epicNames memo (L69) — inline as `(epics || []).map(e => e.name)`
2. Consolidate pendingTasks + backlogTasks + doneTasks (L44-46) into one memo returning { pending, backlog, done }
3. Consolidate doneGroups + backlogGroups (L91-92) into one memo returning { doneGroups, backlogGroups }
4. Result: 12 memos → 8 memos

## Files to modify
- src/components/TaskBoard.tsx — consolidate and remove memos

## Branch
task-116-reduce-taskboard-memos
## Worktree
.devmanager/worktrees/task-116
## Status
- [ ] Remove epicNames memo
- [ ] Consolidate 3 task-filter memos into 1
- [ ] Consolidate 2 grouping memos into 1
- [ ] Verify build passes
